class Customer {
  constructor({ fullName, email, cpf, identity, phone, address }) {
    this.fullName = fullName
    this.email = email
    this.cpf = cpf
    this.identity = identity
    this.phone = phone
    this.address = address
  }
}

export { Customer }
